=>  Bootstrap’s grid system is built with flexbox it uses column property as box to build layout there are certain method's to implement column to  build layout

(1) you have to create container (any size)
(2) inside that container create row

=>  Now what is row 

you can think of row class as a empty div with css property of display flex

(3) in side row you can use col class to create different box

=>  You can also use different breakpoints to decide width of each and every column

=> there are only 12 column you can create inside in row 

